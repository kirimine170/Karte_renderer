# Minimal Karte Format v0.1 package

Copy this directory and change the package identity, layout, styles, theme, and
assets together. The paths in `karte-format.yaml` are relative to this
directory.

This example defines the package contract only. CLI and Go API package
application are introduced separately.
