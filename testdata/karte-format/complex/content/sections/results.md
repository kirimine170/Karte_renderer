## Results

The selected columns must keep their requested order and escape HTML-like data.

@import(type="csv" path="../../data/results.csv" select="status,metric,value")

@import(type="md" path="nested/findings.md")
